// Fallback: acessado em refresh ou link direto sem interceptação.
// Renderiza o mesmo modal de criação na página inteira (sem lista atrás).
export default function CreateTournamentPage() {
  // TODO: renderizar TournamentFormModal mode="create" como página
  return null;
}
